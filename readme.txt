1. Install python3 packages
2. Put IP Address in file_of_ips.txt
3. run ipscrape.py w/ file_of_ips.txt in same directory
4. Will output csv 

This should all work if the correct python packages are installed
May need requests if fails -- pip install requests